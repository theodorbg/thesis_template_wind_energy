%!TEX root = ../Thesis.tex

%---- AMEYA old stuff ----%
	%% Allow relative font size specifications (e.g. \smaller, \larger).
%	\usepackage{relsize}
	
	%% For defining all text/page measurements. 170x240mm is the
	%% standard thesis page size Ponsen & Looijen require. 124x185mm is
	%% simply a text body size that resulted in margins that pleased me.
	%% The dvips option will be superceded if pdflatex is used, but is
	%% necessary in order to generate a correct PostScript bounding box
	%% under standard LaTeX.
%	\usepackage[vcentering,dvips]{geometry}
%	\geometry{papersize={170mm,240mm},total={130mm,197mm}}
	
	%% For printing crop-marks in a final a4 version. This is not used
	%% in the final 170x240mm-sized version to be delivered to Ponsen & Looijen.
%	\usepackage[a4,cam,center,frame,dvips]{crop}
%	\crop[font=\upshape\mdseries\small\textsf]

	%% We will completely define our own header strings, so switch the fancy
	%% headers on, but nuke all the default values. (Note that this package
	%% *has* to load after the geometry package!)
%	\usepackage{fancyhdr}
%	\pagestyle{fancy}
%	\fancyhf{}
	%%% Now begin customising things. See the fancyhdr docs for more info.
%	\renewcommand{\chaptermark}[1]{\markboth{\MakeUppercase{#1}}{}}
%	\renewcommand{\sectionmark}[1]{\markright{\MakeUppercase{#1}}{}}
	%%%%\renewcommand{\sectionmark}[1]{\markright{\thesection.\ #1}}
%	\renewcommand{\headrulewidth}{0pt}

%	\makeatletter
%	\newcommand*\std@chapter{}
%	\let\std@chapter=\@chapter
%	\renewcommand*\@chapter[2][]{\std@chapter[#2]{#2}\chaptermark{#1}}
%	\makeatother
%
%	\let\stdsection=\section
%	\renewcommand{\section}[2][]{%
%	    \ifthenelse{\equal{#1}{}}
%	        {}
%	        {\sectionmark{#1}}%
%	    \stdsection{#2}%
%	    \ifthenelse{\equal{#1}{}}
%	        {}
%	        {\sectionmark{#1}}%
%	    }
	
	%% Control the fonts and formatting used in the table of contents.
%	\usepackage[titles]{tocloft}
	
	%% Because of the font change, the page number becomes too large for the
	%% horizontal space LaTeX reserves for it by default. Without the following
	%% redefines to fix it, this would cause the Chapter entry page numbers
	%% to extend a few points into the right margin. The horror!
%	\makeatletter
%	\renewcommand{\@pnumwidth}{1.75em}
%	\renewcommand{\@tocrmarg}{2.75em}
%	\makeatother
%	%% Aesthetic spacing redefines that look nicer to me than the defaults.
%	\setlength{\cftbeforechapskip}{2ex}
%	\setlength{\cftbeforesecskip}{0.5ex}

%	\usepackage{tikz}
%	\usetikzlibrary{shapes,arrows}
%	 Define block styles
%	\tikzstyle{mybox} = [draw=black, very thick,
%	    rectangle, rounded corners, inner sep=10pt, inner ysep=20pt]
%	\tikzstyle{myboxpreviouschapter} = [draw=gray, very thick,
%	    rectangle, rounded corners, inner sep=10pt, inner ysep=20pt ]
%	\tikzstyle{fancytitle} =[fill=black, text=white]
%	\tikzstyle{fancytitleold} =[fill=gray, text=white
%---------------------------------------------------------------------------------------------
%---- ANTOINE stuff ----%
	%% Make sure that the bibliography is listed in the table of contents, but that the table of contents itself is not.
%\usepackage[nottoc]{tocbibind}
	%% Support for more complex bibiography handling.
%\usepackage[square,authoryear,sort&compress]{natbib}
%% Custom bibliography style defined using the Makebst utility that comes with the natbib package.
%\bibliographystyle{bib/bibleo}
	
	% Figures and captions 
%\usepackage[font=small,format=plain,labelfont=bf,up,textfont=it,up]{caption}
%\usepackage{subfig}
%\usepackage{subcaption} 

	% Lists/Itemize
\usepackage{enumitem}
\setlist{itemsep=3pt,topsep=3pt,parsep=3pt} %Reduce spacing in lists (settings in next line)
%\renewcommand{\labelitemi}{$\bullet$}
%\renewcommand{\labelitemii}{$\cdot$}
%\renewcommand{\labelitemiii}{$\diamond$}
%\renewcommand{\labelitemiv}{$\ast$}
	% uniformly use international system of units
\usepackage{siunitx}
\DeclareSIUnit{\nothing}{\relax}
	% processes the PostScript graphics with psfrag to add labels within the graphic
\usepackage{pstool}
	% Nomenclature
\usepackage{nomencl}
%\usepackage[notintoc]{nomencl}
	% hypertext links 
%\usepackage{hyperref} %% ALREADY loaded in general.tex
%\hypersetup{colorlinks=true,%
%	citecolor=black,filecolor=black,linkcolor=black,urlcolor=black,%
%	pdftex}
%% \newcommand{\MYhref}[3][blue]{\href{#2}{\color{#1}{#3}}}%
\newcommand{\tn}{\textnormal}
	% Verbatim (e.g. for comment environment)
\usepackage{verbatim}
	% color package
\usepackage{color}
	% mathematical features
\usepackage{amsmath,amsfonts,amssymb}
\usepackage{mathrsfs,mathtools} 
\DeclareMathOperator{\atantwo}{atan2}
\DeclareMathOperator{\arctantwo}{arctan2}
\DeclareMathOperator{\atan}{atan}
\usepackage{breqn} % automatic equation breaking
\usepackage{bm} % bold symbolds
	% Tables
\usepackage{multirow}
\usepackage{array}
\usepackage{tabularx}
\usepackage{tabulary}
\usepackage{booktabs}
\usepackage{makecell}
\newcommand\T{\rule[1.2ex]{0pt}{2.6ex}} % Top strut
\newcommand\B{\rule[-1.2ex]{0pt}{0pt}} % Bottom strut
\setlength{\tabcolsep}{6pt}
\renewcommand{\arraystretch}{1}
	% PDF and page layout
\usepackage[final]{pdfpages}
%\usepackage{afterpage}

	% Bibliography 
%% biblatex ALREADY loaded in general.tex
%\usepackage[square,authoryear,sort&compress]{natbib}
%\usepackage[style=authoryear,natbib=true,backend=bibtex]{biblatex} 
%\usepackage[citestyle=authoryear,natbib=true]{biblatex}
%\renewcommand\nameyeardelim{, }